import { Box } from "@chakra-ui/react";

const Placeholder = ({ componentName }) => (
    <Box>The component <strong>{componentName}</strong> has not been created yet.</Box>
);

export default Placeholder;